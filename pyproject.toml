[project]
name = "lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jan Kaczmarzyk", email = "jan.kaczmarzyk@uekat.pl" }
]
requires-python = ">=3.14.7"
dependencies = [
    "pandas>=3.0.5",
]

[project.scripts]
lab = "lab:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
